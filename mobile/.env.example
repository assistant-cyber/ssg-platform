# Backend API base URL
# For local dev: use your Mac's LAN IP (not localhost — the phone needs to reach it)
# Find it with: ifconfig | grep "inet " | grep -v 127.0.0.1
EXPO_PUBLIC_API_URL=http://192.168.1.100:8000
